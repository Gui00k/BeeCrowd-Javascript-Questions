const input = require('fs').readFileSync('stdin', 'utf8');
const lines = input.split('\n');

let [numero] = lines.shift().split(" ").map(item => parseFloat(item));
let positivo = 0 


//float numero; int positivo = 0; int i;

    for (i = 0; i < 6; i++) { (numero); if (numero > 0) { positivo++; } }

    console.log("valores positivos", + positivo);

    
